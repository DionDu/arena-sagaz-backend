# ─────────────────────────────────────────────────────────────────────────────
# Dependências de DESENVOLVIMENTO da API — o que você instala na sua máquina
# para rodar os testes. É o `requirements_api.txt` (runtime) MAIS as
# ferramentas de teste.
#
# Até 2026-07-21 este arquivo carregava também a pilha de ML e notebook
# (numpy, pandas, matplotlib, seaborn, pygame, ipython, jupyter, ipykernel…),
# porque o laboratório de IA morava neste mesmo repositório. O laboratório se
# mudou para `../ia/` e levou as dependências dele junto — veja
# `ia/requirements_tf.txt` e `ia/requirements_gpu.txt`.
#
# Conferido na separação: nenhum arquivo que ficou aqui importa numpy, pandas,
# matplotlib, seaborn ou pygame.
#
# ⚠️ Dependência nova de RUNTIME (a API precisa dela para rodar em produção)
#    entra em `requirements_api.txt`, não aqui — é o `requirements_api.txt`
#    que o Dockerfile instala.
# ─────────────────────────────────────────────────────────────────────────────

# Tudo o que a API precisa para rodar. O `-r` INCLUI o outro arquivo em vez de
# copiar a lista — assim as duas nunca divergem.
-r requirements_api.txt

# ── Testes ───────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
coverage==7.13.5

# Cliente HTTP usado pelo TestClient do FastAPI nos testes de contrato.
httpx==0.28.1
httpcore==1.0.9
certifi==2026.2.25
